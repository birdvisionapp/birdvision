%h3 Allow API #{f.check_box :allow_sso, :class => 'tc-widget fm-check-input bg-ck-med', 'data-target' => '#allow-sso-container'}
#allow-sso-container
  .control-group
    = f.label :client_key, "Client ID", :class => 'control-label'
    .controls
      .help-inline= @client.client_key || "Will be generated by the system"
  .control-group
    = f.label :client_secret, "Client Secret", :class => 'control-label'
    .controls
      .help-inline= @client.client_secret || "Will be generated by the system"
  .control-group
    = f.label :client_url, "Client URL", :class => 'control-label mandatory-field'
    .controls
      = f.text_field :client_url
    .controls
      .icon-info-sign
      .help-inline used when the participant decides to log out of the BVC Rewards page, the participant is taken to the Client's Intranet portal.