.template-header
  %fieldset#id-set-template-header
    %h3 Update Template
= form_for :template, :url=> {:action=>'update'}, :method => :put, :html => {:id => 'formTemplate'} do |f|
  = render :partial => 'form', :locals => {:f => f}
  
